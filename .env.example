# Google Gemini API key — used by the /api/z-chat route (server-side only).
# Get one from https://aistudio.google.com/apikey
# Copy this file to `.env.local` and fill in the value. `.env.local` is gitignored.
GEMINI_API_KEY=
